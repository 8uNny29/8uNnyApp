<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= statusCode %> - Error</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      /* Tambahkan padding dinamis untuk layar lebih kecil */
      .error-container {
        padding: 2rem;
      }

      /* Perbaiki ukuran font untuk layar kecil */
      .error-code {
        font-size: calc(4rem + 2vw);
      }

      .error-message {
        font-size: calc(1rem + 0.5vw);
      }

      .error-url {
        font-size: 0.9rem;
        word-wrap: break-word;
      }
    </style>
  </head>
  <body
    class="d-flex justify-content-center align-items-center vh-100 bg-light"
  >
    <div class="text-center error-container">
      <!-- Error Code -->
      <h1 class="error-code text-danger fw-bold"><%= statusCode %></h1>

      <!-- Error Message -->
      <p class="error-message lead"><%= message %></p>

      <!-- Error URL -->
      <p class="error-url text-muted">
        <strong><%= url %></strong>
      </p>

      <!-- Button to Home -->
      <a href="/login" class="btn btn-primary btn-lg mt-4">Kembali ke Beranda</a>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
